# Cursor Agent Configuration
# Update this file to pin to a specific version
CURSOR_VERSION=latest

# Known-good SHA256 checksums (update when version changes)
# To get checksum: shasum -a 256 /tmp/cursor_install.sh
# CURSOR_INSTALLER_SHA256=<checksum-here>
